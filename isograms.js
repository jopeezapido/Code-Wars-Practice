function isIsogram(str) {
    //Determine if string contains only letters
    //I need something that determines whether the argument passed (str) has no repeating letters
    

    //I need something to split the strings into chunks in an array

    //I need a something to filter out the 
    
    //I need something to test if pattLet is and pattNoRep is met

}


isIsogram( "Dermatoglyphics" )//==true
isIsogram( "aba" )//==false
isIsogram( "moOse" )//== false, ignore letter case